#!/bin/bash
#
# hello-ospool.sh
# My very first OSPool job
#
# print a 'hello' message to the job's terminal output:
python3 train_graph_ppo.py --graph 3-r.txt --out trial1
#
# keep this job running for a few minutes so you'll see it in the queue:

